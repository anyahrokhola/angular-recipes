import { BtnLinkDirective } from './btn-link.directive';

describe('BtnLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnLinkDirective();
    expect(directive).toBeTruthy();
  });
});
