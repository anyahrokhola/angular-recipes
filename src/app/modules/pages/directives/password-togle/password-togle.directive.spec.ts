import { PasswordTogleDirective } from './password-togle.directive';

describe('PasswordTogleDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordTogleDirective();
    expect(directive).toBeTruthy();
  });
});
